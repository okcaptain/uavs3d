name: build

on:
  workflow_dispatch:

jobs:
  linux:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: nttld/setup-ndk@v1
        id: setup-ndk
        with:
          ndk-version: r21e
          add-to-path: false

      - name: Build
        env:
          ANDROID_NDK_HOME: ${{ steps.setup-ndk.outputs.ndk-path }}
        run: |
          curDir=$(pwd)
          echo "curDir=$curDir" >> $GITHUB_ENV
          chmod +x ./version.sh
          ./version.sh
          cd build/android/ndk/jni
          ${{ env.ANDROID_NDK_HOME }}/ndk-build
          ls -al ./
          ls -al ${{ env.curDir }}
          ls -al ${{ env.curDir }}/build/
          ls -al ${{ env.curDir }}/build/android/
          ls -al ${{ env.curDir }}/build/android/ndk/
